#include <iostream>
#include <vector>
#include <queue>
using namespace std;

int main(){
    int num_emps;
    cin>>num_emps;
    int *adj[num_emps];
    
    for(int i=0; i<num_emps; i++){
	arr[i] = (int*)malloc(num_emps*sizeof(int));
    }
}
